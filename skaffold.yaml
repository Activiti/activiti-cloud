apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/activiti/activiti-cloud-notifications-service-graphql:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/activiti/activiti-cloud-notifications-service-graphql:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: activiti-cloud-notifications-service-graphql
        chartPath: charts/activiti-cloud-notifications-service-graphql
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/activiti/activiti-cloud-notifications-service-graphql"
          image.tag: "{{.DIGEST_HEX}}"
