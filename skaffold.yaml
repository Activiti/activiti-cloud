apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/activiti/activiti-cloud-runtime-bundle-service:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/activiti/activiti-cloud-runtime-bundle-service:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: activiti-cloud-runtime-bundle-service
        chartPath: charts/activiti-cloud-runtime-bundle-service
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/activiti/activiti-cloud-runtime-bundle-service"
          image.tag: "{{.DIGEST_HEX}}"
