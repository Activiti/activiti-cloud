apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/activiti/activiti-cloud-acceptance-tests:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/activiti/activiti-cloud-acceptance-tests:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: activiti-cloud-acceptance-tests
        chartPath: charts/activiti-cloud-acceptance-tests
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/activiti/activiti-cloud-acceptance-tests"
          image.tag: "{{.DIGEST_HEX}}"
