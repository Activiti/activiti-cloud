apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/activiti/activiti-cloud-api:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/activiti/activiti-cloud-api:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: activiti-cloud-api
        chartPath: charts/activiti-cloud-api
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/activiti/activiti-cloud-api"
          image.tag: "{{.TAG}}"
